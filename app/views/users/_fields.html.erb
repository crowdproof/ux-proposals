<%= f.input :name %>
<%= f.input :email %>
<%= f.input :photo, label: "Photo of You (this will be used on the website)", required: current_user.photo.blank? %>
<%= f.input :bio, placeholder: 'This will go on the conference website. Be sure to check for typos.' %>
<%= f.input :website, label: "City/State/Country", placeholder: 'One speaking slot will be prioritized for a local Vermonter.' %>
<%= f.input :github, label: "Your GitHub Username" %>
<%= f.input :location, label: "City/State/Country, at least one talk slot is being reserved for a local Vermonter." %>
